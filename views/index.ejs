<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" type="text/css" href="styles/style.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous" />
</head>

<body class="p-3 mb-2 bg-light text-dark">
    <!-- creating the form to get the location -->
    <section class="input">
        <h1>Weather App</h1>
        <form action="/submit" method="post">
          <div class="mb-3">
              <label for="formGroupExampleInput" class="form-label" >Enter your location:
              </label>
              <input type="text" class="form-control" id="formGroupExampleInput"
                  placeholder="Location name" name="location"/>
          </div>
            <!-- this btn is to get the lat and lon -->
            <button value="POST" formaction="/submit" class="btn btn-primary">Submit</button> 
            <!-- this btn is to get the weather information -->
            <button value="POST" formaction="/get-weather" class="btn btn-primary">Weather</button>
        </form>
    </section>
    <!-- this part displays the lat and lon information -->
    <div class="response-area">
      <p>
        <%= locals.latitude %>
      </p>
      <p>
        <%= locals.longitude %>
      </p>
    </div>

    <!-- this part displays the weather information only after you get the lat and lon information -->
    <% if(locals.weather){ %>
    <div class="response-area">
      <h4>
        Weather of
        <%= locals.location %>
      </h4>
      <h6>
        Temperature: 
        <%= weather.temp %>
        °C
      </h6>
      <h6>
        Feels Like: 
        <%= weather.feels_like %>
        °C
      </h6>
      <h6>
        Pressure: 
        <%= weather.pressure %>
        hPa
      </h6>
      <h6>
        Humidity: 
        <%= weather.humidity %>
        %
      </h6>
    </div>
    <% } %>
</body>

</html>